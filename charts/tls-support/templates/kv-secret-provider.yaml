{{- $name := include "kv-certs.fullname" . -}}

apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name:  "{{ $name }}-provider"
spec:
  parameters:
    cloudName: ""
    keyvaultName: {{ .Values.inf.kvcerts.keyVaultName }}
    objects: |-
      array:
        {{- range $v := .Values.inf.kvcerts.secretObjects }}
        - |
          objectName: {{ $v.objectName }}
          objectType: secret 
        {{- end }}           
    tenantId: {{ .Values.inf.kvcerts.tenantId }}
    usePodIdentity: "false"
    useVMManagedIdentity: "true"
    userAssignedIdentityID: {{ .Values.inf.kvcerts.userAssignedIdentityID }}
  provider: azure
  secretObjects:
    {{- range $v := .Values.inf.kvcerts.secretObjects }}
    - data:
      - key: tls.key
        objectName: {{ $v.objectName }}
      - key: tls.crt
        objectName: {{ $v.objectName }}
      secretName: {{ $v.secretName }}
      type: kubernetes.io/tls
    {{- end }}